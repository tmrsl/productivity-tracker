import Box from "@mui/material/Box";
import styled from "styled-components";

export const MobileMenuBlock = styled(Box).attrs({
  sx: {
    display: { xs: "flex", md: "none" },
  },
  mr: "auto",
})``;
