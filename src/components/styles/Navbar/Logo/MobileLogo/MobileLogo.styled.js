import styled from "styled-components";
import Box from "@mui/material/Box";

export const MobileLogoBlock = styled(Box).attrs({
  sx: {
    display: {
      xs: "flex",
      alignItems: "center",
      gap: "4px",
      md: "none",
    },
    mr: 1,
  },
})``;
