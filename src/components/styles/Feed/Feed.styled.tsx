import { Box } from "@mui/system";
import styled from "styled-components";

export const FeedBox = styled(Box).attrs({
  sx: {
    display: "flex",
    justifyContent: "center",
    width: "100%",
    height: "100%",
  },
})``as typeof Box;
